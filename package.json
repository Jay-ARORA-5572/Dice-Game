{
  "name": "dicee",
  "version": "2.0.0",
  "description": "A two-player dice-rolling game with best-of-N tournaments, sound effects, and a local leaderboard.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build:capacitor": "rm -rf www && mkdir -p www && cp -r index.html styles.css index.js js images icons manifest.json favicon.svg service-worker.js www/",
    "android:sync": "npm run build:capacitor && npx cap sync android",
    "android:open": "npx cap open android"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.2",
    "vitest": "^2.1.4"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Jay-ARORA-5572/Dice-Game.git"
  },
  "license": "MIT",
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/core": "^8.4.2"
  }
}
