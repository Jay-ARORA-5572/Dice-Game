# OS files
.DS_Store
Thumbs.db

# Editor directories and files
.vscode/
.idea/
*.swp
*.swo

# Node (in case tooling is added later)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables
.env
.env.local

# Real Firebase credentials — see note in js/firebase-config.js.
# (Firebase web config values aren't secret; they're safe to commit as long
# as your database rules stay locked down. Left here as a comment in case
# you ever want to gitignore it again for a different project.)
# js/firebase-config.js

# Test / build artifacts
coverage/
