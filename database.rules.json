{
  "rules": {
    "rooms": {
      "$roomCode": {
        ".read": true,
        ".write": "auth != null && (!data.exists() || !data.child('uids/player2').exists() || auth.uid == data.child('uids/player1').val() || auth.uid == data.child('uids/player2').val())"
      }
    }
  }
}
